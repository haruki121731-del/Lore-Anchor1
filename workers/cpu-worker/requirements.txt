# =============================================================================
# lore-anchor CPU Worker - Python Dependencies
# CPU-only variant: no CUDA torch, no diffusers (no VAE model needed)
# Mist v2 runs in freq mode (NumPy DCT) — no GPU required
# =============================================================================

# --- Deep Learning (CPU-only build — much smaller image) ---
torch==2.1.2+cpu
torchvision==0.16.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Image Processing ---
Pillow>=10.0.0
numpy>=1.24.0,<2

# --- Queue (BLPOP consumer) ---
redis>=5.0.0

# --- Cloud Storage (R2 via S3 API) ---
boto3>=1.34.0

# --- C2PA Signing ---
c2pa-python>=0.4.0

# --- Database (Supabase status updates) ---
supabase>=2.4.0

# --- Retry logic ---
tenacity>=8.2.0

# --- Utilities ---
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.25.0
